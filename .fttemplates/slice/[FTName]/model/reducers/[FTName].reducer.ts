import { produce } from "immer";
import { <FTName | capitalize>Schema } from '../types/<FTName | lowercasefirstchar>';

export const <FTName|lowercasefirstchar>Reducer = () => {
    const nextState = produce();
    return nextState;
};
